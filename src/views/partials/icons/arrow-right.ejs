<%
  const w = (typeof width !== 'undefined' && width != null) ? width : 24;
  const h = (typeof height !== 'undefined' && height != null) ? height : 24;
%>

<svg
    width="<%= w %>"
    height="<%= h %>"
    viewBox="0 0 24 24"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
>
    <g id="Arrow / Arrow_Right_LG">
        <path
            id="Vector"
            d="M21 12L16 7M21 12L16 17M21 12H3"
            stroke="var(--color-secondary)"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
        />
    </g>
</svg>
